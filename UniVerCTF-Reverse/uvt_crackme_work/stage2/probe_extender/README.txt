probe_extender is optional helper tooling for the CTF.

It does not modify files; it only reads a target and prints stats that can help you
spot 'islands' in mostly-zero containers.

Usage (python):
  python probe_extender.py ../void/zen_void.bin
